module.exports = function (grunt) {
    "use strict";

    grunt.loadNpmTasks("grunt-contrib-less");
    grunt.loadNpmTasks("grunt-contrib-watch");

    grunt.initConfig({
        pkg: grunt.file.readJSON("package.json"),
        less: {
            production: {
                options: {
                    banner: "/*! Generated by Grunt-Contrib-LESS: <%= grunt.template.today('isoDateTime') %> | style.less */\n",
                    strictMath: true,
                    compress: true,
                    cleanCss: true
                },
                files: {
                    "css/style.min.css": "css/style.less"
                }
            },
            development: {
                options: {
                    banner: "/*! Generated by Grunt-Contrib-LESS: <%= grunt.template.today('isoDateTime') %> | style.less */\n",
                    strictMath: true,
                    compress: true,
                    cleanCss: true
                },
                files: {
                    "css/style.min.css": "css/style.less"
                }
            }
        },
        watch: {
            files: "css/*.less",
            tasks: ["less"]
        }
    });

    grunt.registerTask("myLess", ["less:intecmedia"]);
    grunt.registerTask("myWatch", ["watch:intecmedia"]);
};